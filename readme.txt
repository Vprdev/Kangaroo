


Tổng:
+ thiếu hiệu ứng animation trong các element	=>> cái có cái ko, bỏ qua, củng k quan trọng
+ thiếu phần responsive 		=>> đã code, nhưng vẫn thiếu các trường hợp responsive của các loại màn hình ko thông dụng :v
+ thiếu code hỗ trợ css cho các trình duyệt ( coccoc, safari... ) =>> chỉ thiếu phần tương thích với internet explorer, opera, firefox ( opera, firefox chưa check, ko biết có tương thích ko :v)
+ phần hiển thị xem nhanh của từng sản phẩm: thanh ảnh hàng dưới =>> chưa fix
+ thiếu các sự kiện click + scroll trong website ( phần js + jquery ) 
+ thiếu phần nhúng facebook ở cuối phần sản phẩm ( trong khối màu chì)=>>> đã fix cần xem lại code( cái ni thuận làm, t chưa đọc qua, chưa biết ra răng nữa :v)
+ thiếu khối phần tử 2 bên website=>> ok
+ thiếu đường link liên kết bên ngoài website ( fb, yt, gg ...) =>> ok
+ thiếu hiệu ứng cho 8 phần tử nằm dưới phần banner slide chuyển ảnh =>> cái ni khó tí, ko đủ thời gian, bỏ qua :v
+ thiếu thanh chạy theo kích thước trên phần banner chuyển slide lớn: =>> đã thêm. thiếu hiệu ứng pause( không set lại time ) cho slide khi hover


update: + nút next + prev của slide lớn: cần xử lí: chỉ hiện lên khi hover vào slide. =>> đã fix 


Bug:
+ trong phần DANH MỤC SẢN PHẨM : chưa xử lí phần tử bị tràn ra ngoài	=>> đã fix
+ hiệu ứng click chuột chuyển slide của mấy khối slide chuyển ảnh nhỏ ở cuối còn lỗi =>> đã fix



update:	+ trong phần xem sản phẩm css chưa chuẩn kích thước, cần css lại cho giống với trang của thầy. =>> kệ ko quan trọng, khỏi fix :v

k biết còn nữa k :v, chừ chưa tìm ra haha




+ chuyển slide ảnh trong phần xem sản phẩm ( phần slide ảnh nhỏ cuối) =>> chưa fix
+ chiều cao của phần tử bên trong banner left	=>> đã fix
+ hiệu ứng sau khi hết hover của scroll left có vấn đề. có thời gian thì fix tiếp =>> bỏ qua, thầy củng k gắt cái ni lắm

+ hiệu ưngs hover trên thanh menu: cần làm chậm hơn khi hover( thiếu animation) =>> đã fix
+ thiếu animaion khi hover vào các sản phẩm	=>> bỏ qua, ko quan trọng lắm


Bug update:

+ ở chế độ tablet thì title name của các khối sản phẩm bị tràn ra ngoài ( lỗi ni fix dc mà chưa có thời gian, thầy ko gắt cái ni :v)








